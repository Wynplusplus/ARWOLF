Bevy's asset root. ARWOLF loads the user's Wolfenstein 3D data from the
filesystem (see the README), not from here, so this folder is intentionally
empty. It exists because `cargo apk` packages it into the APK.
